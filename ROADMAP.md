# Дорожная карта PLGames VKAdmin

> Бот — это **AI-администратор группы ВКонтакте**: ведёт стену, отвечает участникам,
> модерирует комментарии, считает баллы — и всё разумно, на основе реальных данных.

## ✅ Сделано (июнь 2026)

### Оживление бота
- [x] Callback API подключён и работает (события от VK приходят и обрабатываются)
- [x] Один корректный callback-сервер: секрет совпадает с БД, `message_new` включён
- [x] Идемпотентная настройка callback при подключении группы (больше не плодит дубли)
- [x] Логирование типов событий (видно, что шлёт VK)
- [x] LLM-ключ починен (был 401); health-check при старте
- [x] Секреты захардены (дашборд, JWT, cookies); неблокирующий старт
- [x] Проверено вживую: реальные сообщения → онбординг → ответ LLM; начисление XP

### Контент — «как админ, а не как попало»
- [x] Источник контента — **сайт** `plgames-wow.ru/api/news` (а не GitHub)
- [x] GitHub-путь патч-ноутов убран полностью (давал мусор «Нет коммитов»)
- [x] Защита от мусора: строки-ошибки/«нет данных» не уходят на стену
- [x] Markdown-артефакты (`###`, `**`) — запрет в промпте + авто-стриппер
- [x] Относительные URL картинок резолвятся к домену
- [x] **Свежесть**: отбор по дате (новейшее первым) + фильтр `content_max_age_days`
      (по умолч. 14 дней) — старые новости больше не воскрешаются

### Проактивность
- [x] Дайджест админу в ЛС, публичное приветствие новичков, вехи по участникам
- [x] Модерация комментариев: ключевые слова + AI-токсичность → удаление → 3 страйка → бан
- [x] Геймификация: XP за лайки/репосты/комментарии, репутация (+/-), уровни

## 🔜 Бот-администратор — следующее

- [ ] **Картинки к постам** — групп-токен не грузит фото (VK error 27). Варианты:
      (A) прикреплять ссылку источника → превью-карточка ВК; (B) токен сообщества с правами «Фото»;
      (C) загрузка через юзер-токен админа
- [ ] **Статьи без источника не выдумывать** — ветка `write_article` без URL сейчас галлюцинирует;
      блокировать или строго заземлять на описании группы
- [ ] **Видимость модерации админу** — уведомлять о банах/удалениях, включить в дайджест
      (сейчас бот молча банит — риск ложных срабатываний)
- [ ] **Многошаговый агент** — followup без `tools=`, нельзя сцеплять действия
      («посмотри статистику → запостить про лучшее»)
- [ ] **База знаний / FAQ группы** — факты (правила, расписание) для ответов в ЛС и комментах
- [ ] Ротация секретов: `GITHUB_TOKEN (ghp_)`, `VK_APP_SECRET`, VK-токен

## Фаза — Монетизация
- [ ] Интеграция платежной системы (ЮKassa / Robokassa)
- [ ] Автоматическая выдача VIP после оплаты
- [ ] Система подписок (месяц/квартал/год)
- [ ] Витрина товаров/услуг в боте

## Фаза — Масштабирование
- [x] Мультигрупповой сервис: токены групп в БД, изоляция по `group_id`, OAuth-подключение
- [x] Переход с long-polling на Callback API
- [ ] Переход на PostgreSQL в продакшене
- [ ] Docker Compose для production-деплоя
- [ ] Мониторинг и алерты (логи, ошибки, метрики)
- [ ] CI/CD — автодеплой при пуше в `main`
- [ ] Rate limiting и защита от спама
